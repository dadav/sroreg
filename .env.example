# Database Configuration
DB_SERVER=localhost
DB_PORT=1433
DB_USER=sa
DB_PASSWORD=YourPasswordHere
DB_DATABASE=SRO_VT_ACCOUNT

# Server Configuration
SERVER_PORT=8080

# Base URL (optional - auto-detected from request if not set)
# Set this when using a reverse proxy like Caddy or Nginx
# Examples: https://register.example.com or http://localhost:8080
BASE_URL=

# TLS/HTTPS Configuration (optional)
TLS_ENABLED=false
TLS_CERT=./certs/server.crt
TLS_KEY=./certs/server.key

# SMTP Configuration (for password reset emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@silkroad-server.com
